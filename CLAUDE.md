# 競技プログラミング ディレクトリ規約

## ディレクトリ構成

`典型90/<問題番号3桁>/` の中には以下3ファイルがセットで置かれている:

- `question.md` — 問題文
- `solution.md` — ユーザーが考えた解法・思考過程のメモ
- `<問題番号>.py` — 提出コード（例: `055.py`）

## レビュー時の暗黙ルール

ユーザーが `055.py` のように **解答ファイルだけを指定してレビューを依頼してきた場合でも、同じディレクトリの `question.md` と `solution.md` を必ず併せて読むこと**。

理由:
- コードだけ見ても「何を解こうとしているか」「どこで詰まったか」が分からない
- `solution.md` にはユーザーの思考過程が書かれており、考え方の癖・誤解・改善ポイントを指摘するのに必須
- `question.md` の制約（N, 値の範囲など）を踏まえないと計算量レビューができない

レビュー観点の優先度:
1. 解法の方針が問題に対して妥当か（`question.md` の制約と照らす）
2. `solution.md` に書かれた思考過程の論理的な穴・改善余地
3. コードの正しさ・計算量・可読性

## ユーザー像メモ

学習中で、機械的な思考手順とトリガーワードで教えると効きやすい。詳細は auto memory 側を参照。
