```cpp
int binary_search(int key) {
    int left = 0, right = (int)a.size() - 1; // 配列 a の左端と右端
    while (right >= left) {
        int mid = left + (right - left) / 2; // 区間の真ん中
        if (a[mid] == key) return mid;
        else if (a[mid] > key) right = mid - 1;
        else if (a[mid] < key) left = mid + 1;
    }
    return -1;
}
```
上記のような感じ。
二分探索を一言で言ってしまえば、すでにソートされた配列に対して使える、lognの探索法。線形探索ならnなので大幅に効率が上がる
まず、leftとrightを左端と右端のindexにして、midをleft + (right - left)/2で求めることで、真ん中か真ん中二つのうち左側にする。
それで、midを調べ、違う場合はmidよりもkeyのほうが大きかった場合はleft = mid + 1とすることでmidを含めないで次の探索範囲を決め、小さかった場合はright = mid - 1とすることで同様にmidを含めないで次の探索範囲を決める。